{
  "name": "@customer-ops/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run src"
  },
  "dependencies": {
    "@customer-ops/config": "workspace:*",
    "@customer-ops/logger": "workspace:*"
  },
  "devDependencies": {
    "tsx": "4.21.0"
  }
}
